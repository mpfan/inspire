import React from "react";

import Card from "./Card";

import { PublicAPIData } from "../data";

type Props = {
  apis: PublicAPIData[];
};

const CardContainer = (props: Props) => {
  return <div>CardContainer</div>;
};

export default CardContainer;
